
Option.pm
  lines 543:545 - remmed out

    #   if( -e "$ENV{HOME}/$rcfilename" ) {
    #       return "$ENV{HOME}/$rcfilename";
    #   }

  NOTE: this was done for Windows, which had some problem with periods in filenames

