
Currently all specifications are in bins, this should probably be changed to ms and Hz. 
	[Tried once to make front-end, failed] 

epsmerge - on windows is still wobbly.  Failure usually happens when shelling out to run epsmerge from perl.  Several factors seem to make a difference, but definitely make sure perl is installed and available to the windows machine's path (i.e. in the session where the matlab session is running). 
 

